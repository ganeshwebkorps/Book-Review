<div class="card">
    <h2 class="card-header">Book Details</h2>
    <div class="card-body">
        <h5 class="card-title"> Name :<%= @book.book_name %></h5>
        <h5 class="card-text"> Category :<%= @book.category %></h5>
        <%# <h5 class="card-text"> Writer Name :<%= @writer.name %>
            <%= link_to 'Back', books_path, class:"btn btn-primary" %>
            <%# <%= link_to 'slots', all_slots_path(@book), class:"btn btn-success" %>
            <%# <%= link_to 'sevices', all_service_types_path(@book), class:"btn btn-success"%> 
          <%# <% if current_user.admin? %>
            <%# <%= link_to 'Add Slot', new_slot_path(params[:id]), class:"btn btn-primary"%>
            <%# <%= link_to 'Add Service Types', new_service_type_path(params[:id]), class:"btn btn-primary"%> 
<%#             
         <%# <%end%> 

        <% if current_user.type == "Reviewer" %>
            <%= link_to 'Review', new_review_path(para[:id]), class:"btn btn-info"%> 
        <% else %>
            <%= link_to 'View Reviews', reviews_path, class:"btn btn-info" %>    
        <% end %>
    </div>
    
</div>


