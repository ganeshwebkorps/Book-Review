

<div class="card" style="width: 20rem;">
  <div class="card-body">
<%= form_with model: @review do |f| %>
<div>
         <% if @review.errors.any? %>
          <div style="color: red">
            <p><%= pluralize(@review.errors.count, "error") %> prohibited this course from being saved:</p>
            <ul>  
              <% @review.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
          <% end %>
      </div>
    <div class="form-group mb-3">
        <%= f.hidden_field :book_id, value: params[:id], class:"form-control" %>
    </div>

    <div class="form-group mb-3">
        <%= f.label :discription %>
        <%= f.text_field :discription, class:"form-control"%>
    </div>
           
    <div class="form-group mb-3">
      <%= f.submit class:"btn btn-outline-success btn-sm" %>
    
    </div>

<% end %>

  </div>
</div>